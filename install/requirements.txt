stable-diffusion-cpp-python
Pillow
python-dotenv

# Optional: only needed for mode="caption" jobs (image understanding via
# moondream2 — see conf/models.py and README.md "Understanding an uploaded
# image"). Safe to leave installed even if you never download the vision
# model files; it's a normal pip dependency, not a heavy build step.
llama-cpp-python

# Optional: only needed for mode="img2img" jobs carrying remove_target
# (automatic object segmentation before inpainting — see
# conf/segmentation.py and README.md "Removing a named object"). Installs
# a CPU build of PyTorch; on Linux, prefer the CPU-only wheel index to
# avoid pulling in CUDA packages you don't need for this:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers
torch

# (The actual object-removal repaint step — LaMa — is also part of
# remove_target jobs, but needs no separate package here: it's vendored
# in conf/lama_model.py using the torch/numpy/Pillow already listed above,
# rather than the `simple-lama-inpainting` PyPI package, which pins an
# old pillow/numpy range with no prebuilt wheels for current Python
# versions — see that file's docstring for the full explanation. Nothing
# extra to install for this beyond what's already above.)
